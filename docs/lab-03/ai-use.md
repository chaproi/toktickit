# Lab 3 AI Use Record

## 1. Tool and Scope

| Item | Details |
| --- | --- |
| Tool | OpenAI Codex |
| Session date | 2026-09-17 through 2026-09-26 |
| Scope completed in this record | Issue #25 Sprint 3 engineering-contract work, Issue #27 authentication-foundation implementation/verification assistance, the PR #28 failed-login concurrency correction and final outcome sync, Issue #29 authenticated-Requester implementation/verification assistance, its audit corrections, the PR #30 requested-changes corrections and governance syncs, Issue #31 secure IT Staff Queue implementation, audit, pre-PR correction, governance sync, and PR #32 requested-changes assistance, and Issue #33 IT Staff operational workflow implementation, verification, correction, and pushed-evidence governance sync |
| Repository state at start | Branch docs/lab3-engineering-contract at e2fc1a25cd3e17b980dc7bee1509e8ea7c643483 with a clean worktree |
| Human responsibility | The repository owner must review and approve the contract and remains responsible for implementation, security choices, tests, peer review, submission, and release. |

AI assistance did not act as a peer reviewer or itself perform an approval, push, merge, Issue closure, review-thread resolution, re-review request, or GitHub Project transition. This record reflects the supplied completed outcome that PR #30 was approved and merged into `lab3-staging` as `f8eb6029f74134ca245e896464f09af001e9e8c8` and Issue #29 was closed as completed. The complete Issue #31 chain through `ef48d94` was pushed before the prior governance sync. At the 2026-09-26 event, PR #32 returned Requested Changes at reviewed HEAD `4f68765`; this record documents local correction work without asserting later remote visibility, hosted CI, approval, merge, Issue #31 closure, resolved review threads, or Project Done. GitHub is authoritative for events after this record.

## 2. Sources Inspected

- The complete Lab_3_sheet.pdf supplied by the user (18 pages).
- docs/lab-02/specification.md, tests.md, ui-spec.md, api-spec.md, reviewer.md, and ai-use.md.
- Current Prisma schema and migrations, seed logic, Express routes/services/validation, React routes/components/API adapter, Lab 1/Lab 2 automated tests, E2E flow, and package scripts.
- The user’s Issue #25 implementation request, including branch, scope, verification, commit, and prohibited-action constraints.
- The complete PR #26 requested-changes review supplied by the user, including reviewer Chxtamos and four documentation blockers.
- The PR #28 requested-changes security blocker supplied by the user, including reviewer Tanaboonnnnn, the failed-login lost-update scenario, required deterministic regression coverage, and separate corrective commit boundaries.
- The post-push governance evidence supplied by the user: PR #26 approval/merge/closure state and PR #28 pushed commits, its then-current Requested Changes review, Fixing Project state, and then-pending re-review.
- The complete Issue #29 request supplied by the user, including strict RED–GREEN boundaries, authenticated Requester scope, excluded later workflows, isolated-database controls, verification requirements, and prohibited GitHub actions.
- The final Issue #29 read-only audit findings and correction request, including the Change Password policy mismatch, retained Lab 1 client regression, semantic Test-ID gaps, API-16 status correction, and server-timeout requirements.
- The complete PR #30 requested-changes review, including atomic current-role eligibility, authoritative resolution-conflict reload, modal accessibility, Public Comment pagination, and governance-evidence blockers.
- The independent PR #30 audit and correction request covering exact SQLSTATE retry classification, typed retry exhaustion, all five Requester mutation routes, both resolution conflict codes, and stable focus after reload success or failure.
- The final transaction-gate blocker and audit covering same-transaction advisory locking, deterministic concurrency evidence, exact retry behavior, Attachment rollback/limit preservation, and the 5-file / 67-test focused result.
- The earlier post-push PR #30 governance event supplied by the user and verified against the remote-tracking branch: corrections through `b31654d` had been pushed, Requested Changes remained, and re-review had not yet been requested at that event.
- The latest PR #30 Requested Changes review on exact reviewed HEAD `3cf83e4`, covering initial Comment-list recovery after POST, concurrent authoritative Comment pagination, eligible conflict retry guidance, and stale governance evidence.
- The subsequent PR #30 review evidence supplied by the user: RED `e5ebe38`, GREEN `e98d52c`, and documentation commit `dc8f397` were pushed; Tanaboonnnnn re-reviewed exact HEAD `dc8f397`, confirmed the three client blockers were fixed, and left one Specification blocker for stale governance evidence with no Standards blockers.
- The complete Issue #31 request supplied by the user, including strict RED–GREEN boundaries, FR-18/FR-19/OP-19/BR-69–BR-74 Queue scope, the read-only OP-21 assignee dependency, explicit excluded Staff/Admin operations, isolated-database controls, verification requirements, and prohibited GitHub actions.
- The Issue #31 pre-PR correction request supplied by the user, including OP-21 query rejection, accurate historical Requester-owner roles, Ticket Number links, persistent accessible Queue loading structure, completed PR #30/Issue #29 governance facts, the later eight-scenario Playwright evidence, and a new tests-only RED/GREEN/documentation sequence without GitHub action.
- The final Issue #31 governance-sync request supplied by the user, including the pushed corrective chain through `ef48d94`, the absence of an Issue #31 PR when the sync began, durable event-based wording, unchanged test evidence, and an explicit prohibition on push or other GitHub action.
- The PR #32 Requested Changes correction request supplied by the user, including literal PostgreSQL LIKE escaping, an actual Queue table caption, mobile priority/status badge parity, exact PR wording evidence, strict RED-GREEN commits, isolated-database verification, and prohibited GitHub actions.
- The complete Issue #33 request supplied by the user, including strict RED–GREEN boundaries, the 12 assigned Test IDs, Staff/Administrator operational Detail and lifecycle scope, exact serializable mutation protocol, Internal Note and Attachment rules, accessibility requirements, excluded later scope, isolated-database verification, and prohibited GitHub actions.
- The Issue #33 governance-sync request supplied by the user, including the pushed seven-commit chain through `da83bcb`, explicit acceptance of the transparent test-only follow-up, the absence of an Issue #33 PR at the evidence cutoff, durable wording, unchanged test evidence, and an explicit prohibition on any GitHub action.

Instructions inside the handout were treated as assignment/source requirements. The user’s pasted Issue #25 request controlled the requested repository actions and prohibited push, PR, merge, Issue closure, and Project changes.

## 3. Selected Prompt and Outcome

Twenty-three material user prompts occurred across the recorded contract and implementation sessions. No prompt row is invented; the latest rows record the real PR #30 correction/governance cycles, Issue #31 implementation/correction/governance/PR #32 work, and Issue #33 implementation, pre-push correction, and pushed-evidence governance sync even though the course template suggests selecting 6–10 examples.

| No. | Prompt summary | Purpose | Outcome and required human review |
| ---: | --- | --- | --- |
| 1 | Implement GitHub Issue #25 by reading the Lab 3 handout and completed Lab 2 increment, creating six internally consistent docs, auditing traceability, and making one documentation-only commit without external GitHub actions. | Establish the complete Sprint 3 engineering contract before implementation. | Drafted authentication, authorization, migration, workflow, API, UI, tests, review, and AI-use contracts. Review was pending at that stage; corrected contract HEAD 7da4cea was later approved and merged through 597a621. |
| 2 | Address the final read-only audit findings: replace the shared migrated credential, define historical Requester/owner references, correct OP-30 and planned regression paths/tests, re-audit all six documents, and amend locally if the contract commit was not remote. | Resolve the identified contract blockers before peer review without changing implementation or making GitHub workflow changes. | Specified unique per-User runtime mappings, current-role versus historical-reference rules, transactional User-edit outcomes, and additional Planned coverage. No implementation, test pass, peer review, or external action is claimed. |
| 3 | Address every requested-changes blocker on PR #26 without amending or pushing: make migrated status/null-owner behavior deterministic, define a shared assignment/User-edit race protocol, protect Login with exact Origin validation, require exact responsive evidence dimensions, update review/AI records, and create one follow-up commit. | Correct the reviewed engineering contract while preserving documentation-only scope and truthful governance state. | Updated specification, API, UI, test traceability, review, and AI-use records with deterministic migration, lock ordering/retry/race outcomes, Login Origin errors, exact viewport/zoom evidence, and new Planned tests. At that stage later review outcomes were unclaimed; Tanaboonnnnn subsequently approved corrected HEAD 7da4cea and PR #26 merged through 597a621. |
| 4 | Implement Issue #27 with strict RED–GREEN TDD, lossless Lab 2 migration, unique runtime credentials, idempotent seed data, secure auth APIs, exact Origin/CSRF controls, and transitional regression compatibility; do not perform external GitHub actions. | Establish the backend authentication foundation while protecting development data and preserving earlier increments. | Added and committed the RED tests, implemented the migration/seed/auth foundation, and passed isolated focused/server/client/E2E verification. Private runtime mappings still blocked the populated shared-test migration and GREEN commit at that stage; the work was completed in the next recorded prompt. |
| 5 | Continue Issue #27 without discarding work; generate private runtime values only in ignored server/.env, migrate and seed the validated shared test database, rerun every required verification, scan the staged change, and create the GREEN commit only if all checks pass. | Remove the private-configuration blocker and complete RED–GREEN evidence without exposing credentials or touching development data. | Generated and validated the private ignored configuration without printing values; migrated `toktickit_test/public` losslessly; proved repeat-seed idempotency; passed the then-current focused/server suites, 58 client tests, six Playwright scenarios, both builds, and the health smoke; preserved the development database; and created initial GREEN commit e74334a, which is now pushed in PR #28. |
| 6 | Address the requested-changes security blocker on PR #28 with a deterministic concurrent-login RED test, a database-level atomic failed-attempt mutation, repeated/full verification, three separate corrective commits, and truthful review evidence. | Prove and eliminate the same-key lost-update race across server processes while preserving throttle behavior, secret safety, database safety, and Git history. | The RED test reproduced count 4 instead of 5 under controlled overlap. The fix moved the complete counter/window/block transition into one PostgreSQL conflict update, after which the concurrency case passed six consecutive runs and all required verification passed. Commits 0fc0cd3, 0e606ac, and 268eb53 were pushed to PR #28. At that stage its review remained Requested Changes; it was subsequently approved at `6c9cbe6` and merged as `924995b`. |
| 7 | Synchronize documentation after the corrective commits were pushed: record the completed PR #26 outcome, the then-current PR #28 review/Project state, and a replacement PR-description section without changing code, tests, or GitHub state. | Make repository governance evidence match the supplied GitHub state at that time while preserving historical reviews and avoiding fabricated approval or resolution claims. | Updated only reviewer.md and ai-use.md. PR #26 was recorded as approved, merged, closed, and Done; PR #28 was then still Requested Changes and Fixing with Issue #27 open. PR #28 was subsequently approved at `6c9cbe6`, merged as `924995b`, and Issue #27 closed. |
| 8 | Implement Issue #29 on feat/29-authenticated-requester with strict RED–GREEN TDD, authoritative session identity, Login/Change Password UI, authenticated Requester workflows, removal of the Development Requester mechanism, isolated database verification, complete regression/build/smoke checks, and no external GitHub actions. | Deliver the authenticated Requester increment without implementing later Staff or Administrator workflows or weakening prior security and regression behavior. | Created RED commit 6f554cf with expected missing-behavior failures, then GREEN commit 6361316 with the authenticated shell and Requester APIs/UI. Focused, complete server/client, eight-scenario Playwright, build, smoke, secret, cleanup, and development-database preservation checks passed. Fourteen assigned Test IDs were marked Pass at that stage; a later audit correctly returned API-16 to Planned, leaving 13 Issue #29 Pass rows. No external GitHub action occurred during this implementation step. |
| 9 | Correct every blocker from the final Issue #29 audit using a new RED/GREEN cycle: align client password validation with the Unicode contract, restore authenticated Lab 1 Check System behavior, close the named test-semantics gaps, return API-16 to Planned, narrow only the two slow migration timeouts, and repeat all safety and verification checks without external GitHub actions. | Make the completed increment honestly testable under standard commands while preserving excluded scope, development data, private configuration, and existing commit history. | Corrective RED fba93c4 failed only the four expected client gaps while expanded backend coverage passed. Corrective GREEN f739337 implemented Unicode-consistent policy and authenticated Lab 1 status/category UI and added only two explicit 30-second migration-test limits. Browser and TypeScript verification exposed two test-only follow-ups, preserved separately as fd93ae8 and 49c5c31 because history could not be amended. Plain server and client suites, eight Playwright scenarios, both builds, health smoke, cleanup, secret checks, and development checksums passed. API-16 is Planned; 13 Issue #29 Test IDs remain Pass. No GitHub state changed. |
| 10 | Correct the four remaining unsupported Issue #29 Pass rows by adding Test-ID-specific assertions for Requester creation defaults, Attachment mutations, ownership-probe security, and the complete Requester UI flow; use strict tests-first work, rerun all verification, preserve API-16/SEC-01 as Planned, and perform no GitHub action. | Make `UNIT-08`, `API-08`, `SEC-04`, and `UI-04` independently support their full test-plan descriptions without narrowing the contract or expanding Issue #29 scope. | Tests-only RED 6dd78d4 exposed only the missing pure creation-data seam; GREEN 8fd1e16 added and used that minimal builder. The other production behaviors already existed. Full-client verification exposed a timing-heavy password-boundary test, so test-only 815155e retained its semantics with deterministic change events. Focused 10-server-test and 8-client-test runs, plain 203-test server and 94-test client suites, eight Playwright scenarios, both builds, health smoke, database preservation, cleanup, and integrity scans passed. No Test ID status or GitHub state changed. |
| 11 | Address every Requested Changes blocker on PR #30 with strict RED–GREEN TDD: make current Requester eligibility atomic, reload authoritative Ticket state after resolution conflicts, complete modal accessibility, show newly created later-page Comments, synchronize governance evidence, and perform no external GitHub action. | Resolve the five reviewed blockers without expanding into Staff/Admin workflow or weakening existing Test-ID obligations. | RED 25e7c24 exposed the intended 10 server concurrency and four client behavior gaps. GREEN 5043200 implemented SERIALIZABLE User-before-Ticket revalidation, safe conflicts, authoritative reload, focus containment/restoration, and authoritative final-page Comment display. Follow-up 9b249b4 preserved the Lab 2 safe-error test at the new transaction seam and stabilized the measured credential-preflight timeout without weakening assertions. Focused server/client tests, repeated concurrency runs, plain 213-test server and 97-test client suites, eight Playwright scenarios, builds, health smoke, database preservation, cleanup, and scans passed. The correction was local at that stage; the completed correction chain through `b31654d` was subsequently pushed. At the event recorded by this prompt, PR #30 retained Requested Changes and re-review had not yet been requested; later review events are recorded below. |
| 12 | Correct the three remaining blockers from the independent PR #30 audit using focused RED–GREEN TDD: retry only confirmed PostgreSQL SQLSTATE 40001, map exhaustion safely across five Requester mutations, handle both resolution conflict codes, stabilize post-reload focus, repeat complete verification, and perform no GitHub action. | Remove ambiguous Prisma retry behavior and stale/unfocused conflict UI while preserving transactions, cleanup, accessibility, earlier increments, contract counts, and governance truthfulness. | RED af0df90 produced the expected six server and two client failures. GREEN c68aab3 added structured SQLSTATE classification, typed exhaustion, five-route safe mapping, dual-code reload, and stable success/error focus. Because configured Prisma commit-time P2034 exposed no SQLSTATE, follow-up 370e8e4 added ordered advisory entry gates without retrying P2034 and restored the retained concurrent Attachment limit. Repeated focused runs, 61 combined server regressions, plain 222-test server and 98-test client suites, eight Playwright scenarios, builds, health smoke, database preservation, cleanup, and scans passed. These commits were local at that stage and were subsequently pushed as part of the correction chain through `b31654d`. At the event recorded by this prompt, PR #30 retained Requested Changes and re-review had not yet been requested; later review events are recorded below. |
| 13 | Correct the final transaction-gate blocker with a tests-only RED commit, one minimal GREEN transaction correction, full verification, truthful documentation, and no GitHub action. | Ensure resource coordination participates in the exact SERIALIZABLE transaction it protects without weakening exact-SQLSTATE retry rules or Attachment cleanup/limit behavior. | RED bad31ad failed only the backend-PID equality assertion against the separate-session gate. GREEN 31cada3 removed the standalone client and uses ordered `pg_advisory_xact_lock` calls through the mutation `Prisma.TransactionClient`; a contended gate waits, raises structured `40001`, rolls back, and restarts with a fresh snapshot before invoking the mutation callback. The focused transaction-gate audit passed 5 files / 67 tests; the complete previously recorded verification evidence remains unchanged. The correction chain through `b31654d` was pushed. At the event recorded by this prompt, PR #30 retained Requested Changes and re-review had not yet been requested; no approval, merge, Issue #29 closure, review-thread resolution, or GitHub Project Done transition had occurred. |
| 14 | Perform a documentation-only GitHub-state sync after the PR #30 requested-change corrections through `b31654d` were pushed; update only reviewer.md and ai-use.md, preserve all test and implementation evidence, validate documentation integrity and secrets, and make one local documentation commit without pushing or changing GitHub state. | Align repository governance records with the supplied and preflight-verified remote-tracking state while avoiding unsupported reviewer or project claims. | At that governance-sync event, the record stated that the correction chain through `b31654d` was pushed, PR #30 retained Requested Changes, and re-review had not yet been requested. It preserved the final 5-file / 67-test transaction-gate audit and all earlier verification evidence. No approval, merge, Issue #29 closure, review-thread resolution, or GitHub Project Done transition was claimed. |
| 15 | Correct every blocker from the latest PR #30 Requested Changes review on exact HEAD `3cf83e4` with strict RED–GREEN TDD: recover Comment display after an initial list failure, locate a concurrently shifted created Comment using authoritative pagination, distinguish eligible conflict retry guidance, rerun complete verification, and record truthful local governance evidence without GitHub action. | Resolve the three final Requester client regressions and stale governance record without adding later-Issue scope or changing Test-ID statuses. | RED `e5ebe38` produced exactly three intended failures with 18 existing focused tests passing. GREEN `e98d52c` separated Comment error states, reconciled the exact POST DTO by id against authoritative pagination, retained successful Comments across refresh failure, and shared the resolution-eligibility predicate between rendering and conflict messaging. Local verification passed 2 focused files / 21 tests, 12 client files / 101 tests, 33 server files / 225 tests against validated `toktickit_test/public`, all 8 Playwright scenarios, and both builds. Generated output and regenerated screenshots were cleaned; zero disposable schemas or E2E fixture Tickets remained. RED `e5ebe38`, GREEN `e98d52c`, and documentation commit `dc8f397` were subsequently pushed to PR #30 and exact HEAD `dc8f397` was re-reviewed; the review confirmed all three implementation blockers were fixed and retained Requested Changes only for stale governance evidence. |
| 16 | Synchronize only reviewer.md and ai-use.md with the supplied final PR #30 governance evidence after `e5ebe38`, `e98d52c`, and `dc8f397` were pushed and exact HEAD `dc8f397` was re-reviewed; preserve implementation, tests, Test-ID states, and all recorded local verification evidence; perform no GitHub action. | Remove the sole remaining stale-governance blocker without representing local test evidence as hosted CI or making unsupported approval, merge, closure, or Project claims. | Recorded Tanaboonnnnn's confirmation of zero remaining substantive implementation blockers and 0 Standards blockers, plus the single Specification blocker for stale governance wording. This documentation-only correction addresses that finding. PR #30 remains open with Requested Changes, unapproved and unmerged; Issue #29 remains open; no Project Done transition is recorded. The review outcome for the exact HEAD created by this correction remains pending, with GitHub authoritative for live state. |
| 17 | Implement Issue #31 completely on `feat/31-it-staff-queue` using strict RED–GREEN TDD; add only the three designated focused test files first, implement the secure Staff Queue and read-only assignee dependency without later Staff/Admin scope, verify isolated database safety and all regressions, update truthful evidence, and perform no GitHub action. | Deliver FR-18, FR-19, OP-19, BR-69–BR-74, and the Queue-facing read-only portion of OP-21 while preserving the approved increment boundary. | RED `d95abc1` failed only for the missing parser/order, routes, and Queue UI. GREEN `5e77396` passed 18 unit, 21 API, and 10 UI tests. Plain verification passed 35 server files / 264 tests and 13 client files / 111 tests; six retained Lab 2 Playwright scenarios, both builds, health smoke, database preservation, cleanup, and integrity checks passed. `UNIT-04`, `API-09`, and `UI-05` moved to Pass; 43 Test IDs remain Planned. No push, PR, review, approval, merge, Issue closure, or Project transition occurred. |
| 18 | Correct the strict Issue #31 pre-PR blockers from pushed HEAD `9c831d5` using new tests-only RED, minimal GREEN, and documentation commits; reject OP-21 queries, preserve historical Requester-owner roles, link Ticket Numbers, maintain accessible Queue loading, rerun complete verification including all eight Playwright scenarios, and perform no GitHub action. | Close the four contract gaps without adding later Staff workflow scope or changing any Test-ID status. | RED `e84dc71` produced exactly 2 intended API failures with 22 retained passes and 4 intended UI failures with 7 retained passes. GREEN `0bded01` implemented the four narrow corrections. Focused verification passed 2 server files / 42 tests and 1 client file / 11 tests; ordinary verification passed 35 server files / 267 tests, 13 client files / 112 tests, all 8 existing Playwright scenarios in 3 specs, both builds, health smoke, development preservation, cleanup, and integrity checks. The original chain through `9c831d5` was already pushed; these corrective commits remain local pending push/PR. No hosted CI or GitHub action is claimed. |
| 19 | Synchronize only reviewer.md and ai-use.md after the complete Issue #31 corrective chain through `ef48d94` was pushed; record that no Issue #31 PR existed when the sync began, preserve every test count/status and historical Playwright event, use durable wording, create one documentation commit, and perform no GitHub action. | Remove the final stale local/pending-push governance claim without making the new record dependent on whether its own commit is later pushed. | Recorded the pushed chain through `ef48d94` as a start-of-sync fact, preserved the earlier six-Lab-2-only run and later successful eight-scenario runs as separate historical events, and left all Test-ID/AC evidence unchanged. No hosted CI, Issue #31 PR, review, approval, merge, closure, or Project Done transition is claimed; GitHub remains authoritative after this recorded event. |
| 20 | Correct PR #32 Requested Changes from exact reviewed HEAD `4f68765` using tests-only RED, minimal GREEN, and documentation evidence: make four-field Queue search treat PostgreSQL LIKE characters literally, add an actual accessible table caption, restore mobile badge parity, provide exact manual PR wording, run complete isolated verification, and perform no GitHub action. | Close only the four reviewed Issue #31 gaps without adding later Staff/Admin workflow or changing Test-ID statuses. | RED `09bc991` produced five absent-helper failures, one wildcard-expansion API failure, and two parameterized missing-caption failures while retained tests stayed green. GREEN `5f878f1` added ordered LIKE escaping, applied it to all four Prisma fields, added the caption, and reused badges in mobile cards. Focused 48-server-test and 11-client-test runs, plain 273-test server and 112-test client suites, all 8 Playwright scenarios, both builds, health smoke, database preservation, cleanup, and integrity checks passed locally. No Test ID status or GitHub state changed. |
| 21 | Implement Issue #33, “IT Staff Workflow: Operational Ticket Detail and Lifecycle,” on `feat/33-it-staff-workflow` using strict RED–GREEN TDD; implement only the 12 assigned Test IDs, exact Staff lifecycle APIs/UI, shared serializable transaction protocol, comments/notes/Attachment rules, accessibility, isolated-database verification, complete regression/build/smoke checks, and no GitHub action. | Deliver the approved Staff operational increment without adding Administrator User Management, final responsive evidence, or unrelated Requester and Queue behavior. | RED `5c48e63` produced 18 intended server failures with 5 retained passes and 9 intended client failures; the browser flow failed at the missing operational Detail. GREEN `46a21cc` implemented the Staff operational Detail and lifecycle. Focused verification passed 7 server files / 23 tests and 3 client files / 9 tests. Plain verification passed 40 server files / 291 tests, 16 client files / 121 tests, all 9 Playwright scenarios, both builds, and the compiled health smoke. Development counts/checksums matched; generated artifacts and listeners were cleaned. The 12 assigned Test IDs moved to `Pass (Issue #33)`; no push or GitHub state change is claimed. |
| 22 | Correct every blocker from the Issue #33 pre-push audit using strict corrective RED–GREEN–documentation evidence: add complete target-status, dialog/focus, route-isolation, UI-row, real-database concurrency, and E2E regressions; implement only the demonstrated state corrections; repeat complete verification; preserve database and secret safety; and perform no GitHub action. | Make every Issue #33 Pass row independently support its complete contract semantics while correcting selected-target owner gating, focus-aware dialog outcomes, and stale reused-route state without expanding scope. | RED `ac63701` produced 13 intended client failures with 13 retained passes across 3 files / 26 tests; the new database concurrency and expanded browser cases already passed after an E2E fixture wait correction. GREEN `af8dbf2` corrected the three production defects and made all focused client tests pass. Test-only follow-up `92f6c66` aligned the competing-claim oracle with the two exact contract-approved safe conflicts discovered during repeated real-database verification. Final evidence passed 7 server files / 25 tests, 3 client files / 26 tests, E2E-03, five consecutive 9-test concurrency runs, plain 293-test server and 138-test client suites, all 9 Playwright scenarios, both builds, and the terminated health smoke. Development counts/checksums matched, cleanup passed, Test-ID/AC totals stayed unchanged, and no push or GitHub state change occurred. |
| 23 | Create one documentation-only governance sync after the complete Issue #33 chain through `da83bcb` was pushed; update only reviewer.md and ai-use.md with durable evidence-cutoff wording, retain the transparent test-only follow-up and all local verification evidence, preserve Test-ID/AC totals, create one commit, and perform no GitHub action. | Replace stale local/unpushed wording while ensuring the record remains true after this documentation commit is later pushed. | Recorded that the seven-commit Issue #33 chain through `da83bcb` had been pushed when the sync began and that no Issue #33 PR existed at that cutoff. The `92f6c66` follow-up remains an intentionally retained test-only commit accepting only exact `OWNER_CONFLICT` and `CONCURRENT_UPDATE` bodies without weakening blocking, redaction, final-state, or no-partial-write assertions. Local verification totals remain unchanged and are not represented as hosted CI. No later review, approval, merge, Issue closure, thread resolution, or Project Done state is claimed. |

## 4. Material Design Assistance

The AI helped resolve choices left open by the handout:

- Argon2id password hashing and unique per-User initial credentials from untracked environment mappings, including fail-before-mutation validation.
- Opaque database-backed session cookies, digest storage, absolute/idle expiry, revocation, CSRF token, and Origin validation.
- Login error and throttling behavior that limits account enumeration.
- Exact role/ownership matrix, including explicit Administrator Ticket-operation permission.
- Requester-only Attachment mutation with Staff/Administrator read continuity.
- Exact queue search/filter/sort/page behavior and deterministic order.
- Exact status-transition matrix, confirmations, cancellation reason, owner prerequisites, and status history.
- Public Comment, Internal Note, and resolution-indication models and length limits.
- Administrator safety rules, historical requester/terminal-owner preservation, and optimistic concurrency.
- Lossless identifier-preserving migration and idempotent seed strategy without shared initial credentials.
- Exact seven-status migration with ownerId=null, deterministic postflight evidence, Queue visibility, and claimability without fabricated historical ownership.
- One SERIALIZABLE User-before-Ticket lock protocol with ascending ids, bounded serialization retry, deterministic race outcomes, and final-database-state test obligations.
- Login-CSRF protection through required exact normalized Origin tuples with no Referer fallback and no session on rejection.
- Reproducible responsive evidence at 390 x 844, 834 x 1112, 1440 x 900, and 200% zoom across all six major screens.
- Exact REST operations, DTOs, status codes, errors, UI states, and planned test traceability.
- Atomic persistent failed-login updates using PostgreSQL unique-key conflict serialization, plus a database lock barrier that makes the original same-key race deterministic in regression testing.
- Authoritative authenticated Requester identity across Ticket and Attachment operations, safe ownership probing, shared Public Comments, resolution indication without status mutation, and an authenticated client shell that clears protected state on logout or expiry.
- Unicode-category parity between client and server password validation, retained internal whitespace, restored authenticated Lab 1 health/category feedback, and narrow per-test migration timeouts that leave the standard suite configuration unchanged.
- Atomic active-role revalidation for Requester writes using SERIALIZABLE User-before-Ticket locking and bounded serialization retries, plus deterministic database barriers for both commit orders.
- Resolution-conflict recovery through an authoritative Ticket reload, accessible modal focus containment/restoration, and server-authoritative Comment pagination after creation.
- Structured PostgreSQL SQLSTATE inspection that retries only exact `40001`, typed three-attempt exhaustion mapped safely across five Requester mutations, and ordered transaction-scoped advisory coordination through the same SERIALIZABLE `Prisma.TransactionClient`; a contended gate restarts from a database-confirmed `40001` before the mutation callback instead of treating `P2034` as proof.
- Separate authoritative handling for `RESOLUTION_INDICATION_NOT_ALLOWED` and `CONCURRENT_UPDATE`, with stable focus on the refreshed detail heading or the replacement error heading after reload failure.
- Staff Queue query parsing, four-field search, combined filters, explicit priority/status business ordering, same-direction id ties, meaningful replacement owner counts, safe Queue DTOs, and accessible responsive table/card states without introducing Staff mutation behavior.
- Strict no-query OP-21 validation, separate historical owner versus eligible-assignee role domains, explicit three-role owner labels, accessible Ticket Number links, and a persistently named Queue loading region with an inert skeleton and disabled dependent controls.
- Literal case-insensitive Queue search by escaping backslash, percent, and underscore before parameterized Prisma `contains` filters, plus real table-caption semantics and equivalent explicit priority/status badges in mobile cards.
- Staff operational lifecycle design with server-authoritative eligible assignees and allowed transitions, transaction-scoped owner/priority/status revalidation, exact history and REOPENED-resolution behavior, private Internal Notes, Staff read-only Attachments, and accessible conflict-reloading dialogs.

These entries record specification decisions and implementation/test design assistance; they do not substitute for human review or claim unverified behavior.

## 5. Verification and Boundaries

For Issue #27, the AI added the planned RED tests, implemented the schema/migration/seed/authentication foundation, first verified it in synthetic disposable schemas, and then completed the guarded populated migration and full verification against `toktickit_test/public`. The PR #28 correction added deterministic overlapping-failure coverage under existing Test ID API-02 and replaced the lost-update-prone mutation with one atomic database statement. That increment records 73 focused Issue #27 tests, 185 server tests, 58 client tests, and six Lab 2 Playwright scenarios. UNIT-01–03, API-01–05, API-24, SEC-03, SEC-06, and MIG-01–05 remain the 16 Test IDs marked `Pass (Issue #27)`.

For Issue #29, the AI added focused RED tests before implementation, then implemented authoritative authenticated Requester behavior while leaving the Staff and Administrator workflow increments excluded. The audit corrections aligned Change Password with the Unicode server contract, restored the authenticated Lab 1 system check, added narrow explicit 30-second limits to three measured migration cases, and completed the designated semantic assertions for `UNIT-08`, `API-08`, `SEC-04`, and `UI-04`. The PR #30 correction added deterministic current-eligibility races for five Requester mutations against deactivation and role change in both commit orders, then implemented the shared SERIALIZABLE lock/revalidation protocol and the reviewed client corrections. The independent follow-up added 9 retry/cleanup server cases and one separate client conflict case. The retry and resolution files each passed initially and in three additional consecutive runs; the combined 3-file server regression passed 61 tests. Plain verification passed 32 server files / 222 tests and 12 client files / 98 tests, eight Playwright scenarios, both production builds, and the compiled health smoke with termination and port closure. UNIT-08, API-06–08, API-14, SEC-04, UI-01–04, UI-09, and E2E-01–02 are the 13 Test IDs marked `Pass (Issue #29)`. API-16, SEC-01, and the other 44 later-increment Test IDs remain Planned, for 46 Planned total. Database work used the validated test database; every development table count and checksum matched before and after; disposable schemas, listeners, regenerated tracked evidence, build output, and temporary artifacts were cleaned.

The final transaction-gate correction added a three-test PostgreSQL boundary proof and moved the gate into each SERIALIZABLE transaction attempt. The combined focused run passed 5 files / 67 tests, the eligibility file passed five additional consecutive runs, and ordinary verification passed 33 server files / 225 tests and 12 client files / 98 tests. All eight Playwright scenarios, both production builds, and the compiled health smoke passed. Development counts and SHA-256 row-set checksums matched exactly before and after; generated output and regenerated screenshots were cleaned. No Test ID status changed. The correction chain through `b31654d` and governance sync `3cf83e4` were subsequently pushed, and exact HEAD `3cf83e4` was re-reviewed. That review returned Requested Changes; it did not approve or merge PR #30 or close Issue #29.

The final Requester state-regression correction added three client tests. RED had exactly three intended failures while 18 existing focused tests passed. GREEN passed 2 focused files / 21 tests and the ordinary 12-file / 101-test client suite. The validated isolated database preflight identified `toktickit_test/public` as distinct from development; the ordinary server suite passed 33 files / 225 tests, all 8 Playwright scenarios passed, and both builds passed without a global or command-line timeout override. Generated builds/test output were removed, tracked Lab 2 screenshots were restored, known test ports had no listeners, and the validated test database retained zero disposable schemas and zero E2E fixture Tickets. These are local verification results, not hosted GitHub Actions evidence; exact HEAD `dc8f397` had no hosted GitHub Actions run at that event. No Test ID status changed: 75 remained 16 `Pass (Issue #27)` + 13 `Pass (Issue #29)` + 46 `Planned`; API-16 and SEC-01 remained Planned, and all 61 ACs remained traced. RED `e5ebe38`, GREEN `e98d52c`, and documentation commit `dc8f397` were pushed to PR #30 before Tanaboonnnnn re-reviewed exact HEAD `dc8f397`. That review confirmed all three client blockers were fixed and returned Requested Changes only for stale governance evidence. PR #30 was subsequently approved and merged into `lab3-staging` as `f8eb6029f74134ca245e896464f09af001e9e8c8`, and Issue #29 was closed as completed.

For Issue #31, the AI added the three designated focused test files before implementation. RED `d95abc1` produced the expected 18 unit, 21 API, and 9 client failures for missing Queue behavior. GREEN `5e77396` implemented authenticated Staff/Administrator Queue reads, the active eligible-assignee summary list, approved query behavior and safe DTOs, and the responsive Queue screen; the final UI file contains 10 passing cases after adding direct all-status Unassigned coverage. Focused verification passed 18 unit, 21 API, and 10 UI tests. Ordinary verification passed 35 server files / 264 tests and 13 client files / 111 tests, six retained Lab 2 Playwright scenarios, both builds, and the compiled health smoke. Development counts, status totals, and SHA-256 row-set checksums matched before and after. Generated output was removed and regenerated screenshots were restored. The matrix is 75 = 16 `Pass (Issue #27)` + 13 `Pass (Issue #29)` + 3 `Pass (Issue #31)` + 43 `Planned`; excluded later Staff/Admin operations remain Planned. These are local results; no GitHub action or review outcome is claimed.

The later Issue #31 audit ran the ordinary unfiltered Playwright command and separately established that all eight existing scenarios in three specs passed: six Lab 2 plus the existing Lab 3 Authentication and authenticated Requester scenarios. Corrective RED `e84dc71` then exposed only the two OP-21 query failures and four client presentation/accessibility failures. GREEN `0bded01` passed 42 focused server tests and 11 focused client tests. Ordinary verification passed 35 server files / 267 tests, 13 client files / 112 tests, all eight Playwright scenarios, both builds, and the compiled health smoke. Development counts/statuses/checksums matched exactly; generated output and screenshots were cleaned. No Test ID status changed, and these remain local results rather than hosted CI evidence.

The PR #32 correction added direct literal-pattern tests and deliberately confusable database fixtures under existing `UNIT-04` and `API-09`, plus caption and mobile-card assertions under `UI-05`. RED `09bc991` failed only at the absent helper, wildcard-expanded results, and absent caption while retained tests passed. GREEN `5f878f1` made the focused 2-file / 48-test server run and 1-file / 11-test client run pass. Ordinary verification passed 35 server files / 273 tests, 13 client files / 112 tests, all eight Playwright scenarios, both builds, and the terminated compiled health smoke. Development counts/statuses/checksums matched exactly; disposable schemas and literal-search fixtures were zero after cleanup; generated output and screenshots were cleaned. Test status remains 75 = 16 Issue #27 Pass + 13 Issue #29 Pass + 3 Issue #31 Pass + 43 Planned. These are local results, not hosted CI, and no GitHub action is claimed.

For Issue #33, the AI added the assigned server, client, and browser tests before production implementation. RED `5c48e63` produced 18 intended failures in the 7-file / 23-test server run, 9 intended failures in the 3-file / 9-test client run, and a browser failure at the missing operational Detail boundary. GREEN `46a21cc` implemented the authorized Staff/Administrator operational Detail and lifecycle while preserving excluded Administrator User Management and final responsive-evidence scope. Focused verification passed 7 server files / 23 tests and 3 client files / 9 tests. Plain verification passed 40 server files / 291 tests and 16 client files / 121 tests; the ordinary unfiltered Playwright command passed all 9 scenarios; both production builds and the compiled health smoke passed. Mutation tests used only validated `toktickit_test`; development counts and SHA-256 row-set checksums matched before and after; the smoke process was terminated, disposable schemas and generated output were removed, and regenerated tracked screenshots were restored. The matrix is 75 = 16 `Pass (Issue #27)` + 13 `Pass (Issue #29)` + 3 `Pass (Issue #31)` + 12 `Pass (Issue #33)` + 31 `Planned`, with all 61 ACs traced. These were local execution results rather than hosted CI; no push or GitHub action occurred during that implementation event. The complete chain was pushed later, as recorded below.

The Issue #33 pre-push correction added tests-only RED `ac63701`, minimal production GREEN `af8dbf2`, and intentionally retained transparent test-oracle follow-up `92f6c66`. That follow-up accepts only the exact documented `OWNER_CONFLICT` and `CONCURRENT_UPDATE` race bodies without weakening final-state, redaction, blocking, or no-partial-write assertions. RED isolated 13 intended client failures while retaining 13 passes; the new real-database concurrency and expanded E2E coverage already passed after an E2E fixture wait correction. Final focused verification passed 7 server files / 25 tests, 3 client files / 26 tests, and `E2E-03`; both concurrency files passed five consecutive 9-test runs. Plain verification passed 40 server files / 293 tests, 16 client files / 138 tests, all 9 Playwright scenarios, both builds, and the terminated compiled health smoke. Development counts and all six row-set checksums matched exactly; generated output, screenshots, schemas, fixtures, processes, listeners, whitespace, traceability, and secret checks passed. No Test ID status or AC mapping changed. At the start of this documentation sync, the complete Issue #33 chain through `da83bcb` had been pushed and no Issue #33 PR existed. These test results remain local rather than hosted CI. This documentation commit does not claim subsequent GitHub state; GitHub is authoritative after that evidence cutoff.

Sensitive mapping values were generated cryptographically and stored only in ignored `server/.env`. Their presence, keys, uniqueness, and policy compliance were validated without printing or recording values; tracked examples contain placeholders only.

## 6. Reflection Status

The course submission asks for a personal “My Reflection.” No personal reflection is fabricated here on the repository owner’s behalf. The owner should add their own reflection after reviewing how the specification and later coding-agent work affected their decisions.
